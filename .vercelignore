# Only apps/web is deployed (project Root Directory = apps/web); the rest of
# the repo is dead weight in the upload. Paths are anchored with a leading
# slash so nested directories of the same name (apps/web/scripts) are kept.
node_modules
.next
/packages
/infrastructure
/security
/scripts
/docs
/db
